//
//  ContentView.swift
//  Scheduler
//
//  Created by Joshua Bielas on 1/29/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Changed on UI Branch!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
